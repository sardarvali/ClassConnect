# AttendanceHistoryFragment — View past attendance records

---

## 📁 Location

> ⚠️ This file has not been implemented as a separate fragment. Attendance history is viewed within AttendanceFragment using the AttendanceHistoryAdapter to display past sessions.

The `AttendanceHistoryAdapter.kt` (27 lines) displays a list of past `AttendanceRecord` items with date, present count, and absent count.

