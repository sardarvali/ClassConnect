# QrCodeAnalyzer

> ⚠️ This file has not been implemented as a separate class. QR code scanning in AttendanceFragment uses the `zxing-android-embedded` library's built-in scanner (`IntentIntegrator`) and ML Kit's barcode scanning for camera-based QR reading, rather than a custom analyzer class.

